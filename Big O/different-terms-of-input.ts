function compressBoxesTwice(boxes: string[], boxes2: string[]) {
  // O(n)
  boxes.forEach(function (box) {
    console.log(box);
  });

  // O(m)
  boxes.forEach(function (box) {
    console.log(box);
  });
}

/////////////////////////////////////////
// Time Complexity
//
// O(n + m)
//
// End of Time Complexity
/////////////////////////////////////////
